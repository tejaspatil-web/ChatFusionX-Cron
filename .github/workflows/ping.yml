name: Keep Render Awake

on:
  schedule:
    - cron: "* * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping Render Server
        run: |
          curl -s "$RENDER_PING_URL"
        env:
          RENDER_PING_URL: ${{ secrets.RENDER_PING_URL }}
